# AcmeSync Chat (Next.js)

Customer support chat built with the Next.js App Router and the Vercel AI SDK
(`streamText` + `useChat`). One streaming model call per request in
`app/api/chat/route.ts`.

```bash
npm install
export OPENAI_API_KEY=sk-...
npm run dev
```

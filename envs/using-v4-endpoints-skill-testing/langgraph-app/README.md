# langgraph-app (dummy env)

A minimal single-node LangGraph agent. **Not instrumented with Langfuse** and no
Langfuse dependency yet — starting point for the LangGraph instrumentation dataset item.

- `agent.py` — one-node graph calling an OpenAI model
- `requirements.txt` — no `langfuse` entry

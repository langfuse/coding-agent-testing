# node-service (dummy env)

A minimal Express + TypeScript LLM service. **No `langfuse` dependency yet** — starting
point for the Node SDK-setup dataset item.

- `src/index.ts` — `/chat` endpoint calling OpenAI
- `package.json` — no `langfuse` entry
